[project]
name = "autopilot-adapter-sql"
version = "0.1.0"
description = "SourceAdapter implementation for generic RDBMS sources via SQLAlchemy. Tier 2 coverage"
requires-python = ">=3.11"
dependencies = ["autopilot-core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/autopilot_adapter_sql"]

[tool.uv.sources]
autopilot-core = { workspace = true }
